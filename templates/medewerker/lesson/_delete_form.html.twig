<form method="post" action="{{ path('medewerker_lesson_delete', {'id': lesson.id}) }}" onsubmit="return confirm('Weet u dit zeker?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lesson.id) }}">
    <button class="btn btn-outline-danger btn-sm">Verwijder</button>
</form>
